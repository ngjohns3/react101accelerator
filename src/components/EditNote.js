import React from 'react';

const EditNote = props => (
    <button type="button" onClick={props.enterEditWorkspace} className="button primary header-text">Edit Note</button>
);

export default EditNote;